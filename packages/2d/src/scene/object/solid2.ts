import {Filter} from "../../filter";
import {CompositeOperation} from "../composite-operation";

export class Solid2 {

    filter: Filter = new Filter();

    compositeOperation: CompositeOperation = CompositeOperation.NORMAL;

    // shadow

    // clipping

    // paint (image, rect, text, path)
}